apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app-deployment-gitlab
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-app-gitlab
  template:
    metadata:
      labels:
        app: node-app-gitlab
    spec:
      containers:
        - name: node-app-gitlab
          image: prathmesh2102003/nodejs:1f727572
          ports:
            - containerPort: 3000
          env:
            - name: ENV
              value: "production"
